import XCTest
@testable import SubFramework

class FreeFunctionsTests: XCTestCase {


//    func testExample() throws {
//      let orderedData = (0...100)
//        .map { _ in UUID() }
//      
//      // Test objects for ordering
//      let objects = orderedData
//        .map { Keyword(id: $0, value: randomString(length: 10)) }
//        .reduce({ Set($0) })
//      
//      let orderedKeywordIDs = orderWith(orderedData)(objects)
//        .map(\.id)
//      
//      XCTAssertEqual(orderedData, orderedKeywordIDs)
//      
//    }

    func testPerformanceExample() throws {
        // This is an example of a performance test case.
        self.measure {
            // Put the code you want to measure the time of here.
        }
    }

}
